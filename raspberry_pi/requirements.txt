# ==============================================================================
# AgroVision Raspberry Pi — Python Dependencies
# ==============================================================================

# --- Core (required on all platforms) ---
requests>=2.31.0
python-dotenv>=1.0.0
Pillow>=10.0.0          # Required: OLED framebuffer rendering + SimulatedCamera

# --- Realtime SSE (optional, stdlib fallback exists) ---
sseclient-py>=1.8.0

# ==============================================================================
# Hardware-specific libraries (install on Raspberry Pi / Linux only)
# Run:  pip install -r requirements.txt --extra-index-url ...
# Or use the install.sh script which handles this automatically.
# ==============================================================================

# OLED Display (I2C SSD1306 / SH1106 via luma.oled)
# luma.oled>=3.13.0

# Text-to-Speech (pyttsx3 with espeak backend on Linux)
# pyttsx3>=2.90

# Better TTS (Google TTS — requires internet connection)
# gTTS>=2.5.0

# Speech Recognition (microphone input)
# SpeechRecognition>=3.10.0
# PyAudio>=0.2.14         # Requires: portaudio19-dev OS package

# Raspberry Pi GPIO / I2C (optional, for future sensor support)
# RPi.GPIO>=0.7.1
# smbus2>=0.4.3
